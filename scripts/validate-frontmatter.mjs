#!/usr/bin/env node
// F1 placeholder. Real Ajv-based schema validation se implementa en F2.
// Por ahora: no-op + log para confirmar que el pipeline corre.

console.log('[validate-frontmatter] F1 placeholder — sin content todavía, skipping validation.');
process.exit(0);
